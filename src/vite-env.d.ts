/// <reference types="vite/client" />

/**
 * Vite's ambient client types, which is what makes `import './ui/styles.css'`
 * and `import.meta.env` typecheck. Nothing else belongs in here — every other
 * type in the game is a real declaration in src/core/types.ts.
 */
